{
  "name": "codex-usage-injector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "通过本地 CDP 注入在 Codex 用户名称后展示剩余用量",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/cli.mjs watch",
    "probe": "node src/cli.mjs probe --json",
    "status": "node src/cli.mjs status --json",
    "restore": "node src/cli.mjs remove --json",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/cli.mjs && node --check src/injector.mjs && node --check src/widget-runtime.mjs"
  }
}
